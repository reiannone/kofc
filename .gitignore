# dependencies
web/node_modules/

# build output (optional — drop this line if your deploy serves web/dist directly)
# web/dist/ is committed — the production server has no Node to build with

# secrets
api/config.local.php
docs/OpenAI API Key.txt
*.pem
.env

# os/editor cruft
Thumbs.db
.DS_Store
# never commit secrets
sql/KofC Credentials.txt
*Credentials*
*.secret
